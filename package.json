{
  "package": "where-is-it",
  "devDependencies": {
    "@napi-rs/cli": "2.18.4"
  },
  "napi": {
    "name": "where-is-it"
  },
  "scripts": {
    "build": "napi build --release --features node-compile",
    "build:debug": "napi build --features node-compile"
  }
}
